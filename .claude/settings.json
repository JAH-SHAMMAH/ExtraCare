{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // empty' | grep -q 'git.*commit' && git push origin HEAD || true",
            "statusMessage": "Pushing to origin..."
          }
        ]
      }
    ]
  }
}
